name: Java CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: GitHub Actions for Apache Maven
      uses: actions/checkout@v1
    - run: |
        mvn clean install
        mvn test
        mvn site
        mvn deploy -Dregistry=https://maven.pkg.github.com/nishkarshraj -Dtoken=726c973d702fafebbb268bec774bb6e02ab24105
